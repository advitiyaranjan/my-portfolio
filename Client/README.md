
  # Portfolio Website Frontend

  This is the frontend code for Portfolio Website. A modern, responsive portfolio built with React + Tailwind CSS.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  