{
  "version": 2,
  "buildCommand": "npm --prefix Client run build",
  "public": true,
  "outputDirectory": "Client/dist",
  "installCommand": "npm install --legacy-peer-deps && npm install --include=dev --prefix Client",
  "functions": {
    "api/[...path].js": {
      "runtime": "nodejs22.x",
      "memory": 256,
      "maxDuration": 10
    }
  },
  "env": {
    "JWT_SECRET": {
      "description": "Secret key for JWT tokens - REQUIRED for authentication"
    },
    "NODE_ENV": {
      "description": "Environment mode",
      "default": "production"
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/[...path].js",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"]
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
